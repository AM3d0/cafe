class OrdersService {
  // function to create a new document which contains a blank list where all wanted products will be added
  // At first, the status will be inactive as long as the customer sends the order
  // This is important, so the kitchen wont get a blank order, since a new document is created
  // and the kitchen page has a listener which displays all the orders which are active

  // function which gets the new created documents id

  // function to push an order

  // function to get all the orders, which have an active status

  // function to change the status of an order to inactive, since its done
}
